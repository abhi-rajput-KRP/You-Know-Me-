<h1>You Know Me ? </h1>
<h4>An anonymous Confession platform</h4>

<h2>FEATURES</h2>

<h3>Tech stack</h3>

- Backend: Flask (Python)

- Database & Authentication: Supabase

- Frontend: HTML, CSS

- Deployment: Render

<h3>PROJECT STRUCTURE</h3>

```
You-Know-Me-/
├── .gitignore
├── README.md
├── app.py
├── requirements.txt
├── templates/
│   ├── create_posts.html
│   ├── login_fail.html
│   ├── login.html
│   ├── posts.html
│   ├── register.html
└── .env
```

<h3>SETUP</h3>

### 1. Create a Virtual Environment</h4>

#### 🪟 Windows (CMD or PowerShell)

```
python -m venv venv
venv\Scripts\activate
```

#### 🍎 macOS / 🐧 Linux

```
python3 -m venv venv
source venv/bin/activate
```

### 2. Install Dependencies

`pip install -r requirements.txt`

### 3. Configure Supabase Tables

```
Supabase table structure
-- Table 1: posts (id, name, email, body)
create table if not exists public.posts (
  id bigint generated by default as identity primary key,
  name text not null,
  email text not null,
  body text not null
);

-- Table 2: users (id, name, email)
create table if not exists public.users (
  id bigint generated by default as identity primary key,
  name text not null,
  email text not null
);

-- Disable RLS {I have done it , my code is in accordance to it}
alter table public.posts disable row level security;
alter table public.users disable row level security;

```

### 4. Configure Enviornment Valieables

```
Create .env file and add credentials

SUPABASE_URL = Your-Supabase-project-url
SUPABASE_KEY = Your-project-api-key
```

### 5. Run the Application

`python app.py`
